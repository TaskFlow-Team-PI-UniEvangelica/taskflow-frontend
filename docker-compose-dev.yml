version: '3.8'

services:
  frontend_dev:
    build:
      context: .
      dockerfile: Dockerfile-dev
    container_name: taskflow_frontend_dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules